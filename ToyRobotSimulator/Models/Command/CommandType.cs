namespace ToyRobotSimulator.Models.Command {

    /// <summary>
    /// This enum represent accepted user command.
    /// </summary>
    public enum CommandType {
        PLACE,
        MOVE,
        LEFT,
        RIGHT,
        REPORT
    }
}